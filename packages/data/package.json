{
  "name": "@sleeping-giants/data",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "dev": "tsc -w -p tsconfig.build.json",
    "lint": "eslint \"src/**/*.ts\" \"tests/**/*.ts\"",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@sleeping-giants/shared": "workspace:*",
    "node-html-parser": "^6.1.10",
    "pg": "^8.11.5"
  },
  "devDependencies": {
    "@types/pg": "^8.10.9"
  }
}
